# Download Google Drive Link via curl
- Make sure the permissions of the file is set to `Anyone with the link`
```
curl -L "https://drive.usercontent.google.com/download?id=IDFROMGOOGLEDRIVELINK&export=download&confirm=xxx" -o pic.zip
```
